from flask import Flask, render_template, url_for

app = Flask(__name__)

@app.route("/")
@app.route("/home")
def home():
    return render_template("home.html")

@app.route("/sort")
def sort():
    return"<h1>Sort</h1><p>aha</p>"
if __name__ == "__main__":
    app.run(debug=True)
    #http://localhost:5000/